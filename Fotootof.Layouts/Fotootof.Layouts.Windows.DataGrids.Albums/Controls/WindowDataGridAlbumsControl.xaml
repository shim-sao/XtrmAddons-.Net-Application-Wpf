<base:WindowBaseForm 
    x:Class="Fotootof.Layouts.Windows.DataGrids.Albums.Controls.WindowDataGridAlbumsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dg="clr-namespace:Fotootof.Layouts.DataGrids.Albums.Controls;assembly=Fotootof.Layouts.DataGrids.Albums"
    xmlns:p="clr-namespace:XtrmAddons.Fotootof.Culture.Properties;assembly=XtrmAddons.Fotootof.Culture"
    xmlns:base="clr-namespace:XtrmAddons.Fotootof.Lib.Base.Classes.Windows;assembly=XtrmAddons.Fotootof.Lib.Base"
    mc:Ignorable="d"
    Title="{StaticResource WindowsDataGridAlbumsTitle}"
	ResizeMode="NoResize"
    Width="480"
    Height="480">

    <!-- #region : List Album -->
	<Grid x:Name="GridBlockRootName" Style="{DynamicResource FrameworkElementRootStyle}">

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TabControl x:Name="TabControlFormContentName"
					Grid.Row="0">

            <!-- #region : Tabs Informations -->
            <TabItem Header="{x:Static p:Resources.Albums}">

                <dg:DataGridAlbumsControl x:Name="DataGridAlbumsControlName"
                                          DataContext="{Binding}"
                                          HorizontalAlignment="Stretch"
                                          Grid.Row="0"/>

            </TabItem>
            <!-- #rendegion : Tabs Sections -->

        </TabControl>


        <Border
            Grid.Row="1"
            Grid.RowSpan="2">

            <Grid>
				<StackPanel Style="{StaticResource StackPanelFormValidatorStyle}">

					<Button x:Name="Button_Save"
							Click="DialogSave_Click"
							Content="{x:Static p:Resources.Save}"
							Style="{StaticResource ButtonSaveStyle}"/>

					<Button x:Name="Button_Cancel"
							Content="{x:Static p:Resources.Cancel}"
							Style="{StaticResource ButtonCancelStyle}"/>

				</StackPanel>
            </Grid>
        </Border>

    </Grid>
    <!-- #endregion -->
    
</base:WindowBaseForm>
